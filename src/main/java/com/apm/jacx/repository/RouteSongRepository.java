package com.apm.jacx.repository;

import com.apm.jacx.model.RouteSong;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RouteSongRepository extends JpaRepository<RouteSong, Long> {
}
